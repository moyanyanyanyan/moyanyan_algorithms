# 哈希表总结

## 两数之和

1.哈希用来记住遍历过的数组值与数组下标，每次都拿来查询哈希里面有没有对应的值

## 快乐数

1.用 set 来记住快乐数在迭代中的值，然后可以快速查询去判断有没有循环

## 两个数组的交集

1.本来是去进行双重循环，判断是否相等，如果相等去遍历相等数组，如果已经有就不添加，没有就添加 2.把两个数组都转换成 set，遍历一个 set，每次都在另外一个函数里面用判断的 has 方法，做到时间复杂度的大大减少
